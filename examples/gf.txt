import scas.residue.GaloisField
import scas.base.BigInteger
import BigInteger.given

val r = GaloisField("2")("alpha")
val List(alpha) = r.generators
import r.given
r.update(alpha\2+alpha+1)

assert (alpha\2 >< alpha+1)
