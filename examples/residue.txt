import scas.base.{BigInteger, Rational}
import scas.polynomial.tree.UnivariatePolynomial
import scas.residue.Residue
import BigInteger.given
import Rational.given

val r = UnivariatePolynomial(Rational)("x")
val List(x) = r.generators
import r.given

val s = Residue(Rational(x))(2-x\2)
import s.given

assert (2 >< x\2)
assert (Rational("2") - x\2 >< 0)
assert (s.toString == Rational(x)(2-x\2).toString)
assert (s.math == Rational(x)(2-x\2).math)
