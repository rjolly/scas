import scala.language.{strictEquality, implicitConversions}
import scas.arith.BigInteger
import scas.arith.{given Conversion[Int, BigInteger]}

assert(BigInteger(1) + 1 == 1 +: BigInteger(1))
assert(BigInteger(1) - BigInteger(1) == (0: BigInteger))
